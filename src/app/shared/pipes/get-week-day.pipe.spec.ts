import { GetWeekDayPipe } from './get-week-day.pipe';

describe('GetWeekDayPipe', () => {
  it('create an instance', () => {
    const pipe = new GetWeekDayPipe();
    expect(pipe).toBeTruthy();
  });
});
